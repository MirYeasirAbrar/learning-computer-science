// evaluate square root values- sqrt()
#include <stdio.h>
#include <math.h>
int main(){
    double num = 16.0;
    double sqrt_num = sqrt(num);
    printf("The square root of %f is %f\n", num, sqrt_num);
    return 0;
}