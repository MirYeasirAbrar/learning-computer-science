// evaluate absulate values - abs()
#include <stdio.h>
#include <math.h>
int  main()
{
    int a;
    printf("Enter an integer: ");
    scanf("%d", &a);
    printf("The absolute value of %d is %d\n", a, abs(a));
}